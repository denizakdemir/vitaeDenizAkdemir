% This file contains LaTeX commands to be included in the preamble of all CV templates
% It sets paths to the style files that we've moved to the styles directory

% Set the path to moderncv style files
\makeatletter
\def\input@path{{../../styles/moderncv/}}
\makeatother

% Set the path to fonts
\makeatletter
\def\input@path{{../../fonts/}}
\makeatother

% Define pandocbounded command for compatibility with newer pandoc versions
\providecommand{\pandocbounded}[1]{#1}